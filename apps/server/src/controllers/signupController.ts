import {Request, Response} from "express"


//zod schemas can be moved to root
export const signupController = async (req: Request, res: Response) => {
    try {
        
    } catch (error) {
        
    }
}